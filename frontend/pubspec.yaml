name: frontend
description: "Oto Ekspertiz AI Mobil Uygulaması - AUTO-SCAN PRO"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  image_picker: ^1.0.7
  http: ^1.2.0
  desktop_window: ^0.4.2
  # Tasarımdaki modern yazı tipleri için ekledik
  google_fonts: ^6.2.1
  share_plus: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Eğer ileride ikon veya placeholder eklersek burası hazır kalsın
  # assets:
  #   - assets/